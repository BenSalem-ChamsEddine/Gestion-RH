#spring.application.name=gestion-entretien-service
#spring.cloud.config.uri=http://localhost:8888

spring:
  application:
    name: gestion-entretien-service
  cloud:
    config:
      uri: http://172.17.0.1:8888
      retry:
        maxAttempts: 20
      fail-fast: true