spring:
  application:
    name: proxy-service
  cloud:
    config:
      uri: http://172.17.0.1:8888
      retry:
        maxAttempts: 20
      fail-fast: true
      
eureka:
  client:
    service-url:
      defaultZone: http://172.17.0.1:8761/eureka/
